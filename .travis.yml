language: python
os: linux
notifications:
  email:
    on_success: change

jobs:
  include:
    - stage: "test"
      name: "3.6"
      python: "3.6"
      install:
        - pip install -r requirements.txt
      script:
        - ./test/create_test_config.bash
        - python cli.py config view