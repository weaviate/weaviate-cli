# TEMPORARY: weaviate/weaviate-python-client#1991 (alter schema drop vector index) is
# merged to main but not yet in a released weaviate-client. `update collection
# --drop_vector_index` needs `collection.config.delete_vector_index()` and
# `VectorIndexType.NONE`. Track main until a release ships, then swap to a released
# `weaviate-client>=X`.
weaviate-client @ git+https://github.com/weaviate/weaviate-python-client.git@main
click==8.1.7
twine
pytest
semver==3.0.2
numpy>=1.24.0
build
wheel
importlib_metadata==8.5.0
importlib-resources>=5.0.0
setuptools_scm
black==24.3.0
pre-commit==3.6.2
prettytable>=3.1.0
faker>=20.0.0
matplotlib>=3.7.0
