# TEMPORARY: pinned to weaviate/weaviate-python-client#1991, which adds
# `collection.config.delete_vector_index()` and `VectorIndexType.NONE`, required by
# `update collection --drop_vector_index`. Swap back to a released `weaviate-client>=X`
# once the PR is merged and shipped. Pinned to an exact SHA (not the branch ref) so CI
# installs the validated commit; the branch was rebased onto main v4.23.0, and 4abce144
# makes `delete_vector_index()` return None (the CLI ignores the return).
weaviate-client @ git+https://github.com/weaviate/weaviate-python-client.git@4abce144ea8910cdfa7d60f3d96f379be80c9ec0
click==8.1.7
twine
pytest
semver==3.0.2
numpy>=1.24.0
build
wheel
importlib_metadata==8.5.0
importlib-resources>=5.0.0
setuptools_scm
black==24.3.0
pre-commit==3.6.2
prettytable>=3.1.0
faker>=20.0.0
matplotlib>=3.7.0
